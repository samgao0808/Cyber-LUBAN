# campusCar 新底盘开发文档

> 面向后续开发者、学生小组和现场集成人员。本文说明 `~/campusCar-new-chassis` 当前代码的开发边界、模块职责、接口约束和接入顺序。
>
> English version: [Development_Guide.md](Development_Guide.md)

---

## 项目介绍

`campusCar` 是一套面向校园机器人小车的 ROS2 集成工程。NUC 作为车载主控，统一接入底盘控制、相机视频、RTK/GPS 定位、网页控制台和 UE5 可视化/指令系统。这个新底盘工作区 `~/campusCar-new-chassis` 的目标是把原先旧车上的上层能力复用到新的硬件组合上：STM32 串口直连底盘、Hikrobot GigE 工业相机、RTK 定位和 UE5 联动。

工程重点不是单个算法 demo，而是端到端集成：上层保持稳定的 `/cmd_vel`、`/fix`、`/U2RTopic_Command`、`/R2UTopic_Pos`、HLS/RTSP/MJPEG 视频接口；底层通过 profile 切换不同底盘和相机实现。当前仓库已经完成 profile 骨架、UE/RTK/视频链路保留和 Hikrobot 相机接入入口，下一步重点是补齐 STM32 `direct_uart` 底盘驱动。

---

## AI 辅助部署导览

本节是给 Codex、Claude Code、Copilot Chat 或其他 AI 助手看的快速入口。把下面的提示词复制给 AI，可以让它先读对上下文，再帮你做部署、排错或继续接入。

### 推荐给 AI 的起始提示词

```text
你正在协助部署 /home/hkust-gz-nuc/campusCar-new-chassis。
请先阅读 AGENTS.md、.codex-memory/project-overview.md、.codex-memory/current-context.md、
docs/开发文档.md、docs/快速启动指南.md、docs/部署调试备忘.md 和 docs/UE对接文档.md。

目标是新底盘 STM32 + Hikrobot profile，不要把旧底盘 Orange Pi/Orbbec 的启动路径混进来。
请先检查 git status、config/robot.env、config/profiles/new-stm32-hikrobot.env、
scripts/launch_all.sh、scripts/check_all.sh、scripts/stop_all.sh 和 scripts/hikrobot_camera_probe.sh。
然后给出或执行部署步骤，明确哪些步骤已经可运行，哪些仍因 direct_uart 未接入而只能检查/保护退出。
```

### AI 开始前必须确认

| 检查项 | 命令 | 期望结论 |
|--------|------|----------|
| 当前目录 | `pwd` | `/home/hkust-gz-nuc/campusCar-new-chassis` |
| 当前分支 | `git branch --show-current` | `profile/new-stm32-hikrobot` |
| 工作区状态 | `git status --short --branch` | 看清未跟踪本地文件，不要误删 |
| 当前 profile | `source config/robot.env && echo "$CAR_PROFILE $CAR_CHASSIS_MODE $CAMERA_MODE"` | `new-stm32-hikrobot direct_uart hikrobot_gige_aravis` |
| 新底盘保护状态 | `rg -n "direct_uart|保护|还没有接入" scripts/launch_all.sh` | 看到 `direct_uart` 仍是保护退出 |
| 相机探测入口 | `test -x scripts/hikrobot_camera_probe.sh && echo ok` | 可执行 |

### AI 部署任务分流

| 用户目标 | AI 应优先做什么 | 不应做什么 |
|----------|----------------|------------|
| 首次部署依赖 | 读 `scripts/deploy_dependencies.sh`，确认 ROS2 Humble、rosbridge、mediamtx、Aravis/Hikrobot 依赖 | 不要假设 STM32 底盘已经能由 `launch_all.sh` 拉起 |
| 相机到货调试 | 运行/解释 `./scripts/hikrobot_camera_probe.sh`，检查 `camera_aravis2`、`arv-tool-0.8`、GUID/IP、`camera.log` | 不要把相机输出改成新的全局话题，优先 remap 到 `/camera/color/image_raw` |
| 新底盘串口接入 | 补 `direct_uart` 分支、底盘驱动启动、`check_all.sh` 检查项和 `stop_all.sh` 停止项 | 不要改 UE JSON，不要走旧底盘 SSH 启动 |
| UE 联调 | 核对 `docs/UE对接文档.md`、`src/rosbridge_bson_tcp.py`、`src/ue_bridge.py` 和端口 `9090/8888/8554` | 不要把调试字段塞进 UE 固定业务 JSON |
| RTK/定位调试 | 检查 `/fix`、`/R2UTopic_Pos`、`data/logs/ue5_bridge.log` 和 `UE_PUBLISH_RATE` | 不要让 RTK 缺失阻断 rosbridge、视频和网页控制台 |
| 现场健康检查 | 执行 `./scripts/check_all.sh`，再按日志定位 `camera.log`、`rosbridge.log`、`ue_bridge.log`、`mediamtx.log` | 不要先重构代码，先确认运行事实 |

### AI 可执行的部署顺序

```bash
cd ~/campusCar-new-chassis
git status --short --branch

source config/robot.env
printf 'profile=%s chassis=%s camera=%s\n' "$CAR_PROFILE" "$CAR_CHASSIS_MODE" "$CAMERA_MODE"

./scripts/check_all.sh
./scripts/hikrobot_camera_probe.sh
```

如果只是做相机和 UE/视频部署，可以继续检查端口和话题：

```bash
source config/robot.env
source "$ROS_SETUP"

ros2 topic list
ss -tlnp | grep -E ':8080|:8088|:8554|:8888|:9090'
```

如果任务是“让 STM32 新底盘真正能跑”，AI 应先实现并验证：

1. `scripts/launch_all.sh` 的 `direct_uart` 启动分支
2. 新底盘驱动日志，例如 `data/logs/chassis.log`
3. `scripts/check_all.sh` 对串口、驱动进程和 `/cmd_vel` 订阅方的检查
4. `scripts/stop_all.sh` 对底盘驱动的停止逻辑
5. 最小 `/cmd_vel` 前进/停车测试

### AI 不能越过的保护线

- 不要把 `~/campusCar-old-chassis` 的旧底盘流程复制回新底盘部署路径。
- 不要把 `direct_uart` 保护退出描述成“已完成底盘部署”。
- 不要修改 UE 固定话题和业务 JSON，除非用户明确要求改 UE 协议。
- 不要删除 `.codex-memory/`、本地 `.local.env`、未跟踪 `hoverboard_ws/` 或用户现场文件。
- 不要提交 `data/logs/`、相机图片、build/install 产物或私有配置。

---

## 现场调试安全要求

本节适用于学生小组、现场集成人员和 AI 辅助部署人员。任何让底盘上电、轮子转动、链轮/链条/皮带/齿轮运动、执行机构动作的操作，都必须先满足以下安全要求。

### 0. 总原则

- 不带电操作：调链条、接线、拔插电机线、整理线缆、拆装传动件、调整轮组、安装防护罩前，必须先停车、断底盘主电源，确认运动部件完全停止。
- 不用手挡车：车辆运动异常时，用急停、断电或遥控停车处理，不要用手、脚或身体去挡车。
- 不单人调车：实车落地测试至少安排操作员和安全员，安全员负责盯现场并随时急停。
- 不越级调试：新参数、新程序、新接线第一次测试必须架空轮子、低速点动，再落地低速短距离测试。

### 1. 上电顺序

推荐顺序：

1. 确认车辆架空或测试区域已清场。
2. 打开遥控器/控制端，确认油门、摇杆、急停处于安全位置。
3. 打开 NUC、交换机、相机、RTK 等低风险外设。
4. 最后接通底盘主电源或电机驱动电源。
5. 上电后先做低速点动和停车测试，确认方向、速度上限和急停有效。

不要在底盘主电源已经接通的情况下整理底盘内部线缆、调整链条、插拔电机线或拆装传动机构。

### 2. 断电顺序

推荐顺序：

1. 先发送停车指令，让 `/cmd_vel` 或遥控输入归零。
2. 先断底盘主电源/电机驱动电源，确认轮子、链轮、链条、皮带和执行机构完全停止。
3. 再关闭 NUC、相机、RTK、交换机等外设。
4. 最后关闭遥控器/控制端。

关键点：**先断底盘，再断遥控器**。不要先关遥控器再让底盘继续带电，避免接收机失联、控制信号异常或旧指令残留导致车辆突然动作。

### 3. 调链、接线和机械检查前

进行以下操作前，必须先断底盘主电源，并确认电机驱动不再供电：

- 调整链条、链轮、皮带、齿轮、轮组、联轴器、传动轴；
- 清理卡住的线缆、扎带、异物、杂物；
- 插拔电机线、电池线、大电流接插件；
- 安装或拆除防护罩；
- 调整推杆、舵机、电子锁、贴标机构、喷洒机构；
- 把手伸入底盘内部或靠近任何夹卷点。

如需观察传动机构运行，只能短时间、架空、低速点动观察，旁边必须有人负责急停。

### 4. 电池与充电

- 电池充电必须放在指定电池防爆柜或安全充电区域。
- 充电期间不能无人看守，应定期查看电池、充电器和周边环境。
- 发现鼓包、异味、异常发热、冒烟、火花、充电器异常，立即停止充电并报告负责人。
- 不要在纸箱、书包、桌面杂物、可燃物旁边充电。

---

## 1. 当前工程状态

当前工作区：

```bash
cd ~/campusCar-new-chassis
git branch --show-current
# profile/new-stm32-hikrobot
```

这个仓库是从旧底盘项目拆出来的新底盘工程，目标硬件是：

| 模块 | 新底盘目标 | 当前代码状态 |
|------|------------|--------------|
| 主控 | NUC，ROS2 Humble | 已保留原 ROS2/Python 集成层 |
| 底盘 | STM32 串口直连 | `new-stm32-hikrobot` profile 已建立，`launch_all.sh` 的 `direct_uart` 分支仍是保护退出 |
| 相机 | Hikrobot/Hikvision `MV-CS016-10GC` GigE | `camera_aravis2` 路径已写入脚本，可用 `hikrobot_camera_probe.sh` 探测 |
| RTK | USB 串口 NMEA | 沿用旧工程自动探测和 `/fix` 发布链路 |
| UE5 | rosbridge TCP/BSON + HLS 视频 | 沿用旧工程 UE 话题和 JSON 合同 |

重要结论：

- 当前默认 `CAR_PROFILE=new-stm32-hikrobot`，直接运行 `./scripts/launch_all.sh` 会在底盘 `direct_uart` 分支保护退出，避免误用旧底盘 SSH 流程。
- 新底盘串口驱动尚未接入 `launch_all.sh`。下一步开发重点是补齐 `direct_uart` 启动、停止、检查和 `/cmd_vel` 到 STM32 的控制链路。
- 现有 `docs/快速启动指南.md`、`docs/部署调试备忘.md` 里仍有大量旧底盘 `~/campusCar-old-chassis` 操作说明；做新底盘开发时以本文和当前代码为准，旧底盘运行请回到 `~/campusCar-old-chassis`。

---

## 2. 总体架构

新底盘目标链路：

```text
UE5
  |  rosbridge TCP/BSON :9090
  v
src/rosbridge_bson_tcp.py
  |  /U2RTopic_Command  std_msgs/String(JSON)
  v
src/ue_bridge.py
  |  /cmd_vel  geometry_msgs/Twist
  v
STM32 UART 底盘驱动（待接入 direct_uart）

Hikrobot GigE 相机
  |  camera_aravis2
  v
/camera/color/image_raw
  |--> src/mjpeg_server.py  :8080
  |--> src/rtsp_server.py + mediamtx  :8554 RTSP / :8888 HLS
  |--> src/car_web_gui.py  :8088

RTK USB 串口
  |  nmea_navsat_driver
  v
/fix
  |  src/rtk_tools/u2r_r2u_bridge.py
  v
/R2UTopic_Pos  std_msgs/String(JSON)
```

核心原则：

- 上层 UE/RTK/视频话题尽量保持稳定，底盘和相机差异通过 `CAR_PROFILE`、`CAMERA_MODE`、启动脚本分支处理。
- UE 业务 JSON 合同不要随意改；兼容 UE 传输格式时优先改 `src/rosbridge_bson_tcp.py`。
- 相机输出统一 remap 到 `IMAGE_TOPIC=/camera/color/image_raw`，避免 GUI、推流和 UE 文档分别适配不同相机话题。

---

## 3. 目录职责

| 路径 | 职责 |
|------|------|
| `config/robot.env` | 全局默认配置入口；设置默认 profile、端口、话题、UE 参数、相机参数 |
| `config/profiles/*.env` | 车型差异覆盖层；当前有旧底盘和新底盘两个 profile |
| `scripts/launch_all.sh` | 全栈启动主入口；当前包含旧底盘 `remote_ssh` 和新底盘 `direct_uart` 保护分支 |
| `scripts/check_all.sh` | 全栈状态检查，按 profile 区分网络/串口检查 |
| `scripts/stop_all.sh` | 停止 RTK、视频、相机、GUI、UE bridge 等进程 |
| `scripts/deploy_dependencies.sh` | 安装 ROS/rosbridge/视频/相机依赖；当前仍包含 Orbbec 旧相机工作区逻辑 |
| `scripts/hikrobot_camera_probe.sh` | Hikrobot GigE 相机到货后的最小探测入口 |
| `src/ue_bridge.py` | UE 指令解析、方向控制、坐标导航、速度闭环 |
| `src/rosbridge_bson_tcp.py` | UE rosbridge TCP/BSON 兼容层 |
| `src/rtk_tools/` | RTK 串口、位置回传、路径记录、航点导航 |
| `src/rtsp_server.py` | ROS 图像转 RTSP 推流 |
| `src/mjpeg_server.py` | ROS 图像转 MJPEG 浏览器预览 |
| `src/car_web_gui.py` | 网页控制台和现场状态面板 |
| `mediamtx.yml` | RTSP/HLS 服务配置 |
| `data/logs/` | 运行日志目录，已被 `.gitignore` 忽略 |
| `hoverboard_ws/` | 当前是本地未跟踪工作区；如要作为正式底盘驱动，需要明确纳入源码或写清构建方式 |

---

## 4. Profile 与配置机制

`config/robot.env` 会先设置默认值，再 source 当前 profile：

```bash
CAR_PROFILE="${CAR_PROFILE:-new-stm32-hikrobot}"
CAR_PROFILE_FILE="${CAR_PROFILE_FILE:-${CAMPUSCAR_PROFILE_DIR}/${CAR_PROFILE}.env}"

if [ -f "$CAR_PROFILE_FILE" ]; then
    source "$CAR_PROFILE_FILE"
fi
```

当前 profile：

```text
config/profiles/new-stm32-hikrobot.env
config/profiles/old-orange-pi-orbbec.env
```

新底盘 profile 关键值：

```bash
CAR_PROFILE_NAME="新底盘 STM32 + Hikrobot"
CAR_CHASSIS_MODE="direct_uart"
CAMERA_MODE="hikrobot_gige_aravis"
CAR_BASE_TYPE="stm32_hoverboard_4wd"
CAR_SERIAL_PORT="${CAR_SERIAL_PORT:-/dev/ttyUSB0}"
CAR_SERIAL_BAUD="${CAR_SERIAL_BAUD:-115200}"
```

开发规则：

- 新增车型时，优先新增 `config/profiles/<name>.env`，不要把车型差异散落在 Python 源码里。
- 端口、话题、速度上限、UE 坐标转换参数优先放 `config/robot.env` 或 profile。
- 当前脚本没有自动 source `config/*.local.env`；临时覆盖建议直接在命令前设置环境变量，或显式设置 `CAR_PROFILE_FILE`。
- 不要把密码、真实公网地址、私有 token 提交进仓库。

---

## 5. 启动入口

### 5.1 当前默认行为

```bash
cd ~/campusCar-new-chassis
./scripts/launch_all.sh
```

在当前默认 profile 下会输出类似：

```text
Profile: new-stm32-hikrobot (新底盘 STM32 + Hikrobot)
底盘模式: direct_uart
当前 Profile=... 使用 direct_uart 新底盘，但 launch_all.sh 还没有接入新底盘启动流程。
```

这是预期保护行为，不是脚本崩溃。

### 5.2 状态检查

```bash
cd ~/campusCar-new-chassis
./scripts/check_all.sh
```

`direct_uart` 模式下，检查脚本不会 ping 旧底盘 `192.168.100.2`，而是显示当前串口配置：

```text
串口设备: /dev/ttyUSB0
串口波特率: 115200
```

### 5.3 停止进程

```bash
cd ~/campusCar-new-chassis
./scripts/stop_all.sh
```

会清理 rosbridge、RTK bridge、视频服务、Hikrobot/Aravis 相机节点、GUI 和 UE bridge。

---

## 6. 新底盘接入路线

`scripts/launch_all.sh` 现在的 `direct_uart` 分支只做保护退出。要让新底盘真正能动，建议按这个顺序开发：

1. 确认 STM32 串口协议和设备名  
   记录 `CAR_SERIAL_PORT`、`CAR_SERIAL_BAUD`、帧头、字段、校验方式、急停策略。

2. 明确底盘 ROS2 节点形态  
   推荐输出统一 `/cmd_vel` 接口，内部再转换成 STM32 串口帧。这样 `ue_bridge.py`、网页控制台、键盘控制都不需要改。

3. 补齐启动脚本分支  
   在 `scripts/launch_all.sh` 的 `direct_uart)` 分支中启动新底盘驱动，写日志到 `data/logs/chassis.log` 或明确命名的日志文件。

4. 补齐检查和停止逻辑  
   `scripts/check_all.sh` 要检查串口、驱动进程、`/cmd_vel` 订阅方、必要的反馈话题；`scripts/stop_all.sh` 要能停止驱动。

5. 实车最小运动验收  
   先不接 UE，直接测试：

   ```bash
   source ~/campusCar-new-chassis/config/robot.env
   source "$ROS_SETUP"

   ros2 topic pub --once /cmd_vel geometry_msgs/msg/Twist \
     "{linear: {x: 0.1, y: 0.0, z: 0.0}, angular: {x: 0.0, y: 0.0, z: 0.0}}"

   ros2 topic pub --once /cmd_vel geometry_msgs/msg/Twist \
     "{linear: {x: 0.0, y: 0.0, z: 0.0}, angular: {x: 0.0, y: 0.0, z: 0.0}}"
   ```

6. 再接入 UE 指令链路  
   确认 `/U2RTopic_Command -> ue_bridge.py -> /cmd_vel -> 串口驱动` 全链路正常。

底盘开发不要直接改 UE JSON。UE 只关心 `/U2RTopic_Command`、`/R2UTopic_Pos`、`/R2UTopic_Text` 和视频 URL。

---

## 7. Hikrobot 相机开发

当前预设路线是 `camera_aravis2`：

```bash
cd ~/campusCar-new-chassis
./scripts/hikrobot_camera_probe.sh
```

探测脚本会检查：

- `camera_aravis2` ROS2 包
- `arv-tool-0.8`
- Aravis 设备枚举
- `ros2 run camera_aravis2 camera_finder`

如果能看到相机 GUID 或 IP，先固定配置：

```bash
export HIKROBOT_CAMERA_GUID=<GUID或IP>
export CAMERA_MODE=hikrobot_gige_aravis
```

常用参数在 `config/robot.env`：

```bash
HIKROBOT_PIXEL_FORMAT=BGR8
HIKROBOT_WIDTH=1440
HIKROBOT_HEIGHT=1080
HIKROBOT_FPS=30.0
HIKROBOT_GEV_PACKET_SIZE=
```

相机输出应统一进入：

```text
/camera/color/image_raw
/camera/color/camera_info
```

后续视频链路不区分 Orbbec/Hikrobot：

| 服务 | 地址 |
|------|------|
| MJPEG 预览 | `http://<NUC_IP>:8080/` |
| RTSP 调试 | `rtsp://<NUC_IP>:8554/robot_cam` |
| HLS 给 UE | `http://<NUC_IP>:8888/robot_cam/index.m3u8` |
| 网页控制台 | `http://<NUC_IP>:8088/` |

排查顺序：

1. `./scripts/hikrobot_camera_probe.sh`
2. `ros2 topic list | grep camera`
3. `ros2 topic hz /camera/color/image_raw`
4. `tail -n 80 data/logs/camera.log`
5. `tail -n 80 data/logs/mjpeg_server.log`
6. `tail -n 80 data/logs/rtsp_server.log`
7. `tail -n 80 data/logs/mediamtx.log`

如果 `PixelFormat` 报错，先把 `BGR8` 改成 `RGB8`；仍失败再用：

```bash
arv-tool-0.8 --name=<GUID或IP> features
```

查看相机实际支持的 GenICam 参数和值。

---

## 8. UE 接口约束

UE 对接以 `docs/UE对接文档.md` 为准。开发时必须保持以下接口稳定：

| 方向 | 话题/端口 | 类型 |
|------|-----------|------|
| UE 发控制 | `/U2RTopic_Command` | `std_msgs/String`，内容是 JSON 字符串 |
| 小车发位置 | `/R2UTopic_Pos` | `std_msgs/String`，内容是 JSON 字符串 |
| 小车发回复 | `/R2UTopic_Text` | `std_msgs/String`，内容是 JSON 字符串 |
| rosbridge | `tcp://<NUC_IP>:9090` | TCP/BSON |
| UE 视频推荐 | `http://<NUC_IP>:8888/robot_cam/index.m3u8` | HLS |
| RTSP 调试 | `rtsp://<NUC_IP>:8554/robot_cam` | RTSP over TCP |

UE 指令内层 JSON 示例：

```json
{
  "commandId": "010",
  "commandType": "move",
  "RobotId": "Robot",
  "RobotType": " ",
  "commandParams": {
    "destination": "Forward",
    "speed": "30"
  }
}
```

`src/rosbridge_bson_tcp.py` 已做两类兼容：

- UE 把业务 JSON 作为 BSON dict 发来时，转成 `std_msgs/String.data` 字符串。
- UE 发了额外加引号的 JSON 字符串时，去掉外层包装。

不要在底盘开发中修改 UE 业务 JSON 字段；调试信息应写日志，不应塞进 UE 固定字段。

`/R2UTopic_Pos` 当前会包含基础字段和 `vehicle` 扩展字段。基础字段必须保持：

```json
{
  "status": 0,
  "status_name": "FIX",
  "latitude": 22.00000000,
  "longitude": 113.00000000,
  "altitude": 10.0,
  "timestamp": 1710000000.0,
  "frame_id": "gps"
}
```

`vehicle` 是附加状态，用于 GUI/调试；如果 UE 侧没有消费，不要强迫 UE 依赖它。

---

## 9. RTK 与导航链路

RTK 串口由 `launch_all.sh` 自动从这些路径探测：

```text
/dev/serial/by-id/*
/dev/ttyACM*
/dev/ttyUSB*
```

找到 NMEA `$GNGGA` 后启动：

```bash
ros2 run nmea_navsat_driver nmea_serial_driver \
  --ros-args -p port:=<SERIAL_PORT> -p baud:=115200
```

位置回传链路：

```text
/fix
  -> src/rtk_tools/u2r_r2u_bridge.py
  -> /R2UTopic_Pos
```

重要参数：

```bash
UE_PUBLISH_RATE=1.0
RTK_POSITION_HOLD_ENABLED=1
RTK_POSITION_HOLD_SPEED_THRESHOLD_MPS=0.03
RTK_POSITION_HOLD_ODOM_TIMEOUT_SEC=2.0
VEHICLE_HEADING_OFFSET_DEG=56.9
```

`/R2UTopic_Pos` 默认 1Hz 固定输出，经纬度按 8 位小数格式输出。RTK 串口缺失时，rosbridge 和 UE 指令通道仍应继续启动；不要把 UE 控制链路硬绑定到 RTK 是否存在。

路径记录和航点导航：

```bash
source ~/campusCar-new-chassis/config/robot.env
source "$ROS_SETUP"

python3 src/rtk_tools/path_recorder.py
python3 src/rtk_tools/gps_navigator.py --waypoints <路径文件.json>
```

---

## 10. 控制与速度闭环

`src/ue_bridge.py` 负责把 UE 指令转换成 `/cmd_vel`：

- `destination="Forward"`：前进
- `destination="TurnBackward"`：后退
- `destination="TurnLeft"`：左转
- `destination="TurnRight"`：右转
- `destination="Stop"`：停车
- `destination={"x": ..., "y": ...}`：坐标导航

速度参数：

```bash
MAX_LINEAR_SPEED=1.0
MAX_ANGULAR_SPEED=1.0
UE_DIRECTION_RATE_HZ=10
UE_DIRECTION_TIMEOUT_SEC=0.8
UE_NAV_RATE_HZ=10
UE_ARRIVE_THRESHOLD_M=0.5
UE_SPEED_KP=0.8
UE_SPEED_KI=0.3
UE_SPEED_ODOM_TIMEOUT=1.0
UE_NAV_MIN_SPEED=0.08
UE_NAV_DECEL_DIST=1.5
```

方向控制和坐标导航会优先用 `/odom` 做速度闭环；`/odom` 缺失或超时会退化为开环。新底盘接入时最好让底盘驱动发布 `/odom`，否则速度闭环无法发挥作用，但控制链路仍能运行。

---

## 11. 日志与排查

运行日志统一写到：

```text
data/logs/
```

常用日志：

| 日志 | 用途 |
|------|------|
| `camera.log` | Orbbec 或 Hikrobot/camera_aravis2 相机节点 |
| `mjpeg_server.log` | MJPEG 预览服务 |
| `rtsp_server.log` | ROS 图像转 RTSP |
| `ffmpeg_rtsp.log` | ffmpeg 编码推流细节 |
| `mediamtx.log` | RTSP/HLS 服务 |
| `rosbridge.log` | rosbridge TCP/BSON |
| `ue5_bridge.log` | RTK/UE 数据桥 |
| `u2r_command.log` | 最近 UE 指令 |
| `ue_bridge.log` | UE 指令到 `/cmd_vel` 控制 |
| `web_gui.log` | 网页控制台 |

常用检查命令：

```bash
./scripts/check_all.sh

source config/robot.env
source "$ROS_SETUP"

ros2 topic list
ros2 topic hz /camera/color/image_raw
ros2 topic hz /R2UTopic_Pos
ros2 topic echo /cmd_vel
ss -tlnp | grep -E ':8080|:8088|:8554|:8888|:9090'
```

---

## 12. 开发规范

### 12.1 改代码前先确认边界

先确认自己在新底盘仓库：

```bash
pwd
# /home/hkust-gz-nuc/campusCar-new-chassis

git status --short --branch
```

旧底盘开发应去：

```bash
cd ~/campusCar-old-chassis
```

不要把两个工作区的 profile、桌面入口、底盘假设混在一起。

### 12.2 保持接口稳定

修改这些内容时，必须同步更新文档：

- 端口：`8080`、`8088`、`8554`、`8888`、`9090`
- UE 话题：`/U2RTopic_Command`、`/R2UTopic_Pos`、`/R2UTopic_Text`
- 相机统一图像话题：`/camera/color/image_raw`
- `/cmd_vel` 控制语义
- UE JSON 字段和速度解释
- `launch_all.sh` 启动顺序

### 12.3 配置优先于硬编码

新增硬件差异时，优先放到：

```text
config/robot.env
config/profiles/<profile>.env
```

Python 源码里不要直接写死某辆车的 IP、串口名、相机 GUID、速度上限。

### 12.4 生成产物不入库

这些路径默认不应提交：

```text
data/logs/
data/recorded_paths/
data/camera_images/
orbbec_ws/
src/rtk_tools/rosbridge_ts/
__pycache__/
*.pyc
```

当前本地的 `hoverboard_ws/` 也不是已跟踪源码。若决定把它作为新底盘驱动来源，需要先整理成清晰的源码目录、构建脚本和文档，再提交。

---

## 13. 推荐分工

| 小组 | 主要目标 | 关键文件 |
|------|----------|----------|
| 底盘控制 | 接入 STM32 UART，补齐 `direct_uart`，发布/订阅 `/cmd_vel` 和 `/odom` | `scripts/launch_all.sh`、`scripts/check_all.sh`、`scripts/stop_all.sh`、`config/profiles/new-stm32-hikrobot.env` |
| 感知视频 | 调通 Hikrobot GigE，相机话题统一到 `/camera/color/image_raw`，验证 HLS/MJPEG | `scripts/hikrobot_camera_probe.sh`、`src/mjpeg_server.py`、`src/rtsp_server.py`、`mediamtx.yml` |
| RTK 导航 | 调通 `/fix`、路径记录、航点导航、位置回传和静止锁点 | `src/rtk_tools/`、`config/robot.env` |
| UE 对接 | 保持 rosbridge、指令 JSON、位置 JSON、HLS URL 稳定 | `src/rosbridge_bson_tcp.py`、`src/ue_bridge.py`、`docs/UE对接文档.md` |
| 控制台 | 网页控制台状态展示、人工接管、日志可读性 | `src/car_web_gui.py`、`src/car_gui.py` |

---

## 14. 新功能验收清单

底盘 `direct_uart` 接入完成前：

- `./scripts/check_all.sh` 能显示新 profile 和串口配置。
- `./scripts/hikrobot_camera_probe.sh` 能完成依赖检查。
- `docs/开发文档.md`、`docs/快速启动指南.md`、`docs/部署调试备忘.md` 对新底盘状态描述一致。

底盘 `direct_uart` 接入完成后：

- `./scripts/launch_all.sh` 不再保护退出，能启动新底盘驱动。
- `ros2 topic echo /cmd_vel` 能看到控制台/UE 发出的速度指令。
- 实车小速度前进和停车测试通过。
- 如果底盘支持反馈，`/odom` 正常发布；如果不支持，文档明确速度闭环退化为开环。
- `./scripts/check_all.sh` 能检查新底盘驱动进程、串口和关键 ROS2 话题。
- `./scripts/stop_all.sh` 能停止新底盘驱动。
- 网页控制台 `http://<NUC_IP>:8088/` 能人工接管。
- UE 通过 `tcp://<NUC_IP>:9090` 发指令，小车响应。
- UE 能收到 `/R2UTopic_Pos`，视频 HLS `http://<NUC_IP>:8888/robot_cam/index.m3u8` 可播放。

---

## 15. 常见误区

- 不要在 `~/campusCar-new-chassis` 里继续调旧 Orange Pi/Orbbec 底盘；旧底盘去 `~/campusCar-old-chassis`。
- 不要把 `new-stm32-hikrobot` profile 的保护退出当成运行失败；这是当前尚未接入串口驱动的安全状态。
- 不要为了调底盘改 UE JSON。底盘变化应隐藏在 `/cmd_vel` 和底盘驱动之后。
- 不要把 RTK 是否插上作为 UE 控制链路启动条件；无 RTK 时仍应能打开 rosbridge、视频和控制台。
- 不要让相机节点输出新话题后要求所有消费者跟着改；优先 remap 到 `IMAGE_TOPIC=/camera/color/image_raw`。
- 不要提交本地日志、build/install 产物、相机截图和私有配置。
