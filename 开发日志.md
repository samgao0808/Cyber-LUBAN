# 算法组开发日志

> 最后更新：2026-07-02（多目标捡球仿真完成，40/40 全部成功）

---

## 一、已完成

### 视觉模块

| 模块 | 文件 | 完成度 |
|------|------|:---:|
| YOLO11 Nano 目标检测 | `algorithm/src/vision/yolo_detector.py` | 100% |
| 针孔相机坐标变换 | `algorithm/src/vision/coordinate_transform.py` | 100% |
| 多目标筛选 | `algorithm/src/vision/target_selector.py` | 100% |
| 视觉 Pipeline 主流程 | `algorithm/src/vision/vision_pipeline.py` | 100% |
| 仿真数据集生成器 | `algorithm/src/vision/simulated_dataset_generator.py` | 100% |

- **模型**：`algorithm/models/balls_trash/best.pt`，YOLO11 Nano，3 类（乒乓球/网球/垃圾）
- **训练结果**：mAP50 99.3%，mAP50-95 82.9%，精确率/召回率 >98%，30 epoch
- **可视化输出**：`algorithm/models/balls_trash/results.png`（训练曲线）、`confusion_matrix.png`、验证集预测图

### MVP 闭环 Pipeline（视觉→控制→发布）

| 模块 | 文件 | 完成度 |
|------|------|:---:|
| 比例控制器（增强版） | `algorithm/src/navigation/mvp_controller.py` | 100% |
| ROS 桥接模块 | `algorithm/src/navigation/ros_bridge.py` | 100% |
| MVP 主流程 | `algorithm/src/mvp_pipeline.py` | 100% |
| 2D 捡球仿真器 | `algorithm/src/simulator.py` | 100% |
| 多目标演示脚本 | `algorithm/multi_target_demo.py` | 100% |

- **控制策略**：增强版比例控制，慢速接近、防卡住脱困、平滑过渡
- **仿真结果**：40/40 全部成功（100%），2球/3球/4球均通过
- **运行命令**：`python multi_target_demo.py --balls 3 --save`

### 导航模块（A* + DWA，复杂场景保留）

| 模块 | 文件 | 完成度 |
|------|------|:---:|
| 栅格地图构建 | `algorithm/src/navigation/map_builder.py` | 100% |
| A* 全局路径规划 | `algorithm/src/navigation/global_planner.py` | 100% |
| 机器人运动学模型 | `algorithm/src/navigation/robot_state.py` | 100% |
| 导航 Pipeline 主流程 | `algorithm/src/navigation/navigation_pipeline.py` | 100% |
| 可视化输出 | 上述文件内置 | 100% |

- **仿真场景**：网球场（边界+球网+周围障碍物，核心场景）、简单场景（3 障碍物，快速测试）、校园（代码保留但非核心方向）
- **可视化图片**：`algorithm/test/output/navigation_*_map.jpg`（全局路径）、`navigation_*_trajectory.jpg`（实际轨迹）

---

## 二、待修复 / 待优化

### 导航

| 问题 | 严重程度 | 描述 |
|------|:---:|------|
| DWA 局部规划卡住 | 🔴 高 | 机器人沿 A* 路径走一段后陷入局部最优，原地旋转，无法到达终点。**MVP 阶段已绕过，改用比例控制** |

### 测试环境

| 参数 | 设定 | 说明 |
|------|------|------|
| 地面颜色 | 绿色 | 乒乓球场地面 |
| 球体颜色 | 白色 | 白色乒乓球 |
| 场景 | 封闭球场，无动态障碍物 | 假设无遮挡 |

> 后续真实数据集微调时，需按此环境生成/采集绿色地面+白色球的训练图片。当前仿真数据集背景可能不匹配，微调时需关注。

### 视觉

| 问题 | 严重程度 | 描述 |
|------|:---:|------|
| 缺少真实数据集 | 🟡 中 | 当前全用仿真数据训练，需用球场实拍照片微调，提升泛化能力 |
| 相机标定 | 🟢 低 | 需标定海康相机内参，替换坐标变换中的默认参数 |

### 待开发

| 功能 | 优先级 | 描述 |
|------|:---:|------|
| 视觉→导航联调 | 🔴 高 | 目标检测输出坐标 → 导航接收 → 规划走到目标点，打通完整闭环 |
| S 形全覆盖扫描 | 🟡 中 | 球场捡球方向的核心功能，类似扫地机器人全覆盖路径 |
| 多目标循环导航 | 🟡 中 | 看到多个球 → 排序 → 逐个走过去 → 捡完返回 |
| 球场边界线检测 | 🟡 中 | 传统图像处理，检测白色标线，用于视觉定位 |
| ROS 节点封装 | 🟢 低 | 发布 `/cmd_vel`（`geometry_msgs/Twist`），与电控组对接 |

---

## 三、重要决策记录

| 日期 | 决策 | 理由 |
|------|------|------|
| 07-02 | **正式确定项目方向：球场捡球机器车**，舍弃校园巡检方向 | 难度低，不需要 RTK/GNSS，相机是核心传感器，封闭场地可控，S 形全覆盖扫描 |
| 07-02 | 与电控组接口：`/cmd_vel` + `Twist` | 行业标准，算法发速度指令，电控组转电机转速 |
| 07-02 | 机械+电控先合，算法最后加入联调 | 传感器需供电，不能完全独立；底盘能动后再装车一把梭 |
| 07-02 | 不使用 UE5 和前端网站 | MVP 阶段核心闭环优先，视觉→导航→控制打通比花活重要 |
| 07-02 | **以 `开发文档-2.md` 为工程化参考基线** | 官方 ROS2 工程模板，Profile/日志/安全规范/架构可直接复用 |

---

## 四、目录结构

```
Cyber LUBAN/
├── 算法/                          # 旧目录（已废弃）
├── algorithm/                     # 算法主目录
│   ├── src/
│   │   ├── vision/                # 视觉源码
│   │   │   ├── vision_pipeline.py
│   │   │   ├── yolo_detector.py
│   │   │   ├── coordinate_transform.py
│   │   │   ├── target_selector.py
│   │   │   └── simulated_dataset_generator.py
│   │   └── navigation/            # 导航源码
│   │       ├── navigation_pipeline.py
│   │       ├── global_planner.py
│   │       ├── local_planner.py
│   │       ├── map_builder.py
│   │       ├── robot_state.py
│   │       ├── mvp_controller.py    # MVP 比例控制器
│   │       └── ros_bridge.py        # ROS 桥接模块
│   ├── mvp_pipeline.py              # MVP 主流程
│   ├── config/                    # 配置文件
│   │   ├── vision_config.yaml
│   │   └── navigation_config.yaml
│   ├── models/balls_trash/        # 训练好的模型
│   ├── dataset/                   # 数据集
│   ├── test/                      # 测试脚本
│   │   └── output/                # 测试输出（可视化图片）
│   └── docs/                      # 文档
├── runs/                          # YOLO 训练运行结果
├── 开发日志.md                    # 本文件
├── 文件说明.md                    # 文件说明
└── 硬件物品清单.md                # 硬件清单
```

---

## 五、关键事件

| 日期 | 事件 |
|------|------|
| 07-01 | 完成 YOLO11 Nano 仿真训练，mAP50 99.3% |
| 07-01 | 完成视觉 Pipeline 完整闭环（图像→检测→坐标→筛选） |
| 07-01 | 完成 A* 全局路径规划，三种仿真场景验证通过 |
| 07-01 | 完成 DWA 局部规划初版，发现狭窄通道卡住 bug |
| 07-02 | 完成导航可视化输出（地图+路径+轨迹） |
| 07-02 | 确定与电控组接口方案（`/cmd_vel` + `Twist`） |
| 07-02 | 完成项目进展汇报 PPT 稿（~450 字） |
| 07-02 | 打包算法模块提交版（`算法模块_提交版.zip`） |
| 07-02 | 正式确定项目方向为球场捡球，舍弃校园巡检 |
| 07-02 | 引入 `开发文档-2.md` 作为工程化参考基线 |
| 07-02 | 创建开发日志（本文件） |
| 07-02 | **搭建 MVP Pipeline**：比例控制器 + ROS 桥接 + 主流程，实现"看到球→走过去→停下"最简闭环 |
| 07-02 | **确定测试环境**：绿色地面 + 白色球体（乒乓球场），无动态障碍物 |
| 07-02 | **项目整理**：清理冗余文件，归档调试脚本，合并重复代码 |
| 07-02 | **多目标捡球仿真**：2D 仿真器 + 增强控制器，40/40 全部成功（100%） |