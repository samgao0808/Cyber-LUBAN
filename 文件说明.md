# Cyber LUBAN 算法模块文件说明

## 目录结构总览

```
Cyber LUBAN/
├── algorithm/          # 算法核心代码、配置、数据集、模型、测试
│   ├── config/         # 配置文件
│   ├── src/            # 源代码
│   │   ├── vision/     # 视觉模块（目标检测 + 坐标变换）
│   │   └── navigation/ # 导航模块（路径规划 + 局部避障）
│   ├── models/         # 训练好的模型权重与训练结果
│   ├── dataset/        # 仿真数据集
│   ├── test/           # 测试脚本与输出
│   └── docs/           # 文档
└── runs/               # YOLO 训练/验证运行输出
    └── detect/val/     # 验证集结果可视化
```

---

## 一、algorithm/config/ — 配置文件

| 文件 | 说明 |
|------|------|
| `vision_config.yaml` | 视觉模块配置（模型路径、相机参数、检测阈值等） |
| `camera_config.yaml` | 相机内外参配置（焦距、安装高度、俯仰角等） |
| `navigation_config.yaml` | 导航模块配置（机器人尺寸、A* 参数、DWA 参数等） |

---

## 二、algorithm/src/vision/ — 视觉模块源码

| 文件 | 说明 |
|------|------|
| `vision_pipeline.py` | 视觉主流程：串联检测 → 坐标变换 → 目标选择 |
| `yolo_detector.py` | YOLO11 模型封装，目标检测推理 |
| `coordinate_transform.py` | 针孔相机模型：像素坐标 → 车体三维坐标 |
| `target_selector.py` | 多目标筛选（最近/最高置信度/优先级） |
| `detector.py` | 检测器抽象基类 |
| `mock_detector.py` | 模拟检测器（用于无模型时的调试） |
| `color_blob_detector.py` | 颜色斑点检测器（备选方案） |
| `simulated_dataset_generator.py` | 仿真数据集生成器 |
| `train_yolo.py` | YOLO 模型训练脚本 |

**核心算法**：YOLO11 Nano（目标检测）+ 针孔相机模型（坐标变换）

---

## 三、algorithm/src/navigation/ — 导航模块源码

| 文件 | 说明 |
|------|------|
| `navigation_pipeline.py` | 导航主流程：地图 → 规划 → 避障 → 输出速度指令 |
| `global_planner.py` | A* 全局路径规划算法 |
| `local_planner.py` | DWA（动态窗口法）局部避障算法 |
| `map_builder.py` | 栅格地图构建（支持网球场/简单两种场景，校园场景保留但非核心方向） |
| `robot_state.py` | 机器人位姿管理、差速运动学模型 |

**核心算法**：A*（全局规划）+ DWA（局部避障）

---

## 四、algorithm/models/ — 训练好的模型

| 路径 | 说明 |
|------|------|
| `yolo_balls_trash.pt` | 最终训练模型（YOLO11，三分类：乒乓球/网球/垃圾） |
| `balls_trash/weights/best.pt` | 训练过程中最佳权重 |
| `balls_trash/weights/last.pt` | 最后一个 epoch 的权重 |
| `balls_trash/results.png` | 训练过程曲线（loss、mAP、precision、recall） |
| `balls_trash/results.csv` | 训练指标数据（CSV 格式） |
| `balls_trash/confusion_matrix.png` | 混淆矩阵（分类结果统计） |
| `balls_trash/confusion_matrix_normalized.png` | 归一化混淆矩阵 |
| `balls_trash/BoxPR_curve.png` | 精确率-召回率曲线 |
| `balls_trash/val_batch*_pred.jpg` | 验证集检测效果可视化 |
| `balls_trash/train_batch*.jpg` | 训练集样本可视化 |

**训练结果**：mAP50 = 99.3%，mAP50-95 = 82.9%，精确率/召回率 > 98%

---

## 五、algorithm/dataset/simulated/ — 仿真数据集

| 路径 | 说明 |
|------|------|
| `images/` | 仿真生成图片（430 张，含乒乓球/网球/垃圾） |
| `labels/` | YOLO 格式标注文件（每张图片对应一个 txt） |
| `dataset.yaml` | 数据集配置文件（类别定义、路径） |

---

## 六、algorithm/test/ — 测试脚本与输出

| 文件 | 说明 |
|------|------|
| `test_vision_pipeline.py` | 视觉 Pipeline 测试 |
| `test_yolo_pipeline.py` | YOLO Pipeline 测试 |
| `test_navigation.py` | 导航模块综合测试 |
| `test_coordinate_transform.py` | 坐标变换测试 |
| `test_simulated_dataset.py` | 仿真数据集测试 |
| `visualize_nav.py` | 导航可视化生成脚本 |
| `output/` | 测试输出目录，包含： |
| `output/yolo_pipeline_vis.jpg` | 视觉 Pipeline 可视化输出 |
| `output/navigation_simple_map.jpg` | 简单场景 A* 路径 |
| `output/navigation_tennis_map.jpg` | 网球场场景 A* 路径 |
| `output/navigation_campus_map.jpg` | 校园场景 A* 路径（非核心，代码保留） |
| `output/navigation_simple_trajectory.jpg` | 简单场景实际轨迹 |
| `output/navigation_tennis_trajectory.jpg` | 网球场场景实际轨迹 |
| `output/navigation_campus_trajectory.jpg` | 校园场景实际轨迹（非核心，代码保留） |
| `output/simulation_output_vis.jpg` | 仿真输出可视化 |
| `output/yolo_simulation_output.csv` | YOLO 仿真输出数据 |

---

## 七、algorithm/docs/ — 文档

| 文件 | 说明 |
|------|------|
| `算法开发路线图.md` | 算法开发整体规划文档 |

---

## 八、runs/detect/val/ — YOLO 验证运行结果

| 文件 | 说明 |
|------|------|
| `confusion_matrix.png` | 验证集混淆矩阵 |
| `BoxPR_curve.png` | 精确率-召回率曲线 |
| `val_batch*_pred.jpg` | 验证集检测效果图 |

---

## 当前状态总结

- **项目方向**：球场捡球机器车（网球场/乒乓球场），舍弃校园巡检方向
- **视觉模块**：YOLO11 检测 + 坐标变换已完成闭环，仿真精度 mAP50 99.3%
- **导航模块**：A* 全局规划已可用，DWA 局部跟踪待优化
- **对控接口**：通过 `/cmd_vel` 话题发布 `geometry_msgs/Twist` 速度指令
- **待优化**：DWA 局部避障在狭窄通道处存在卡住问题
- **工程参考**：`开发文档-2.md` 为官方 ROS2 工程模板，Profile/日志/安全规范可直接复用